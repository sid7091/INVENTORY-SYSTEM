interface:
  display_name: "INVENTORY SYSTEM"
  short_description: "Repo-specific patterns and workflows for INVENTORY-SYSTEM"
  default_prompt: "Use the INVENTORY-SYSTEM repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true